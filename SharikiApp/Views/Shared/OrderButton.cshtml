@model string
@{
    Layout = null;
}
<!-- Button trigger modal -->
<div>
    <br />
    <button type="button"
            class="btn btn-primary btn-sm"
            data-toggle="modal"
            data-target="#orderModal"
            data-whatever="@Model">
        Заказать
    </button>
</div>
